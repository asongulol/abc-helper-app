export default function Home() {
  return (
    <main style={{ padding: '4rem', fontFamily: 'system-ui' }}>
      <h1>ABC Payroll — parallel rewrite</h1>
      <p>
        Phase 1: money core ported and under test (see <code>src/lib/pay</code>). Screens arrive in
        Phase 3.
      </p>
    </main>
  );
}
