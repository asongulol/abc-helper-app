{
  "$comment": "Manual record of which repo migrations have had their (additive) DDL applied to the SHARED PROD DB (cgsidolrauzsowqlllsz). Prod's migration history is DISJOINT from this repo — prod schema was conformed via the shared-prod work (PRs #13-15), so `supabase db push` is NEVER run against prod. The Vercel deploy ships CODE but NOT schema. When you add a migration the deployed code depends on, run its DDL on prod via the Supabase SQL Editor / MCP FIRST (additive migrations are backward-compatible, so applying before the code deploy is always safe), then add its version prefix to `applied` below. scripts/check-prod-migrations.mjs blocks pushing to `main` while any migration newer than `baselineThrough` is unrecorded.",
  "baselineThrough": "00000000000021",
  "baselineNote": "Migrations 00000000000000–00000000000021 predate the prod-conformance baseline and are treated as applied (prod schema was conformed separately; not independently re-verified per migration).",
  "applied": {
    "00000000000022": "2026-06-27 — off-cycle pay: payments.off_cycle_php, off_cycle_pay_items, service_sessions paid_* cols, payments_period_open_enforce trigger update",
    "00000000000023": "2026-06-27 — time_entries.client_company_id (hours→client attribution)",
    "00000000000024": "2026-06-27 — pay_periods.kind ('regular'|'off_cycle') + open-batch index (off-cycle batch payroll)",
    "00000000000025": "2026-06-28 — companies.holidays_config jsonb (server-side observed holidays; applied via MCP)",
    "00000000000026": "2026-07-01 — off_cycle_pay_items basis CHECK extended with 'salaried_hours' (FT/PT catch-up hours; applied via MCP)",
    "00000000000027": "2026-07-01 — pay_period_summaries security_invoker view (period batch-list aggregation in Postgres; applied via MCP)",
    "00000000000028": "2026-07-01 — pg_trgm + audit_log action/entity GIN trigram indexes (audit search; applied via MCP)",
    "00000000000029": "2026-07-12 — fix contractor self-insert of service_sessions: contractor_insert RLS EXISTS(worker_companies/companies) → company_id IN my_clients() (raw EXISTS read admin-only tables, always false under contractor role) + worker_id default my_worker_id(); applied via MCP",
    "00000000000030": "2026-07-13 — allocate_invoice_no() #047: scan max((split_part(invoice_no,'-',2))::int)+1 over ALL year rows (incl. void) instead of count()+1 over non-void, so voiding the year's highest invoice no longer strands allocation on a duplicate-key loop. Replaced prod's prior count-based impl with the repo's max-based+advisory-lock version (behaviorally identical on current data: 1 invoice, 0 voids). Applied via MCP.",
    "00000000000031": "2026-07-28 — workers_wise_recipient_id_uniq + workers_wise_recipient_uuid_uniq partial unique indexes (RP-55: one bank account belongs to one contractor). Pre-flight found ZERO duplicates on either column, so it applied clean. Applied via MCP as prod migration 'workers_wise_recipient_unique'.",
    "00000000000032": "2026-07-28 — pay_periods.include_ha / include_13 / prior_payments (RP-20 single-row rebuild replays the batch's Calculate toggles; RP-23 moves the recalc-undo snapshot server-side). Applied via MCP as prod migration 'pay_period_calc_memory'. src/db/types.ts's hand-edit verified against freshly generated types — identical, no regeneration needed.",
    "00000000000033": "2026-07-28 — pay_date correction for periods locked before RP-03. ⚠️ NOT applied as written: its predicate (pay_date = period_end) matched 16 prod rows, but 14 are imported history (bulk-created 2026-06-04, locked_at null) whose payments.paid_at sits AT or just before period_end — e.g. all 23 rows of 2024-11-01→11-15 were paid on 11-15 — so period_end IS their pay date and the arrears rule would have post-dated real paid runs by two weeks. Prod ran it with an added `locked_at is not null` (RP-03 lived in lockPeriod, so only an app-locked period can carry the bad write): 2 rows changed, 2026-06-01→06-15 06-15→06-30 and 2026-06-16→06-30 06-30→07-15, both within two days of their modal paid_at (06-29 / 07-13). 33's committed text was left unedited because migration history is append-only; migration 35 carries the narrowing and repairs any DB where 33 ran wide. Applied via MCP as prod migration 'backfill_pay_date_arrears'.",
    "00000000000034": "2026-07-28 — payments.computed_gross_php (RP-07: the engine gross behind a manual override, so ↺ can restore it). Applied via MCP as prod migration 'payments_computed_gross_php'. Backfill from the legacy note hit 3 of 1111 rows — the only overridden rows in prod; two recovered a genuinely different computed gross (36290 vs stored 31290, 35954.86 vs 38300), the third had already been collapsed to its override (25000) by the second-save bug. The other 287 non-null notes are 'Historical import (Hubstaff daily report)' and are NOT overrides.",
    "00000000000035": "2026-07-28 — narrows 33 to app-locked periods and restores period_end on any `paid` period this app never locked (imported history) that 33 moved. No-op on prod (33 already ran narrowed there); verified after applying — 49 imported-history rows intact, 0 app-locked rows still carrying period_end, the open period keeps its arrears date. Applied via MCP as prod migration 'pay_date_arrears_app_locked_only'."
  }
}
