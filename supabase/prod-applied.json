{
  "$comment": "Manual record of which repo migrations have had their (additive) DDL applied to the SHARED PROD DB (cgsidolrauzsowqlllsz). Prod's migration history is DISJOINT from this repo — prod schema was conformed via the shared-prod work (PRs #13-15), so `supabase db push` is NEVER run against prod. The Vercel deploy ships CODE but NOT schema. When you add a migration the deployed code depends on, run its DDL on prod via the Supabase SQL Editor / MCP FIRST (additive migrations are backward-compatible, so applying before the code deploy is always safe), then add its version prefix to `applied` below. scripts/check-prod-migrations.mjs blocks pushing to `main` while any migration newer than `baselineThrough` is unrecorded.",
  "baselineThrough": "00000000000021",
  "baselineNote": "Migrations 00000000000000–00000000000021 predate the prod-conformance baseline and are treated as applied (prod schema was conformed separately; not independently re-verified per migration).",
  "applied": {
    "00000000000022": "2026-06-27 — off-cycle pay: payments.off_cycle_php, off_cycle_pay_items, service_sessions paid_* cols, payments_period_open_enforce trigger update",
    "00000000000023": "2026-06-27 — time_entries.client_company_id (hours→client attribution)",
    "00000000000024": "2026-06-27 — pay_periods.kind ('regular'|'off_cycle') + open-batch index (off-cycle batch payroll)",
    "00000000000025": "2026-06-28 — companies.holidays_config jsonb (server-side observed holidays; applied via MCP)",
    "00000000000026": "2026-07-01 — off_cycle_pay_items basis CHECK extended with 'salaried_hours' (FT/PT catch-up hours; applied via MCP)",
    "00000000000027": "2026-07-01 — pay_period_summaries security_invoker view (period batch-list aggregation in Postgres; applied via MCP)",
    "00000000000028": "2026-07-01 — pg_trgm + audit_log action/entity GIN trigram indexes (audit search; applied via MCP)",
    "00000000000029": "2026-07-12 — fix contractor self-insert of service_sessions: contractor_insert RLS EXISTS(worker_companies/companies) → company_id IN my_clients() (raw EXISTS read admin-only tables, always false under contractor role) + worker_id default my_worker_id(); applied via MCP",
    "00000000000030": "2026-07-13 — allocate_invoice_no() #047: scan max((split_part(invoice_no,'-',2))::int)+1 over ALL year rows (incl. void) instead of count()+1 over non-void, so voiding the year's highest invoice no longer strands allocation on a duplicate-key loop. Replaced prod's prior count-based impl with the repo's max-based+advisory-lock version (behaviorally identical on current data: 1 invoice, 0 voids). Applied via MCP.",
    "00000000000031": "2026-07-28 — workers_wise_recipient_id_uniq + workers_wise_recipient_uuid_uniq partial unique indexes (RP-55: one bank account belongs to one contractor). Pre-flight found ZERO duplicates on either column, so it applied clean. Applied via MCP as prod migration 'workers_wise_recipient_unique'.",
    "00000000000032": "2026-07-28 — pay_periods.include_ha / include_13 / prior_payments (RP-20 single-row rebuild replays the batch's Calculate toggles; RP-23 moves the recalc-undo snapshot server-side). Applied via MCP as prod migration 'pay_period_calc_memory'. src/db/types.ts's hand-edit verified against freshly generated types — identical, no regeneration needed.",
    "00000000000033": "2026-07-28 — pay_date correction for periods locked before RP-03. ⚠️ NOT applied as written: its predicate (pay_date = period_end) matched 16 prod rows, but 14 are imported history (bulk-created 2026-06-04, locked_at null) whose payments.paid_at sits AT or just before period_end — e.g. all 23 rows of 2024-11-01→11-15 were paid on 11-15 — so period_end IS their pay date and the arrears rule would have post-dated real paid runs by two weeks. Prod ran it with an added `locked_at is not null` (RP-03 lived in lockPeriod, so only an app-locked period can carry the bad write): 2 rows changed, 2026-06-01→06-15 06-15→06-30 and 2026-06-16→06-30 06-30→07-15, both within two days of their modal paid_at (06-29 / 07-13). 33's committed text was left unedited because migration history is append-only; migration 35 carries the narrowing and repairs any DB where 33 ran wide. Applied via MCP as prod migration 'backfill_pay_date_arrears'.",
    "00000000000034": "2026-07-28 — payments.computed_gross_php (RP-07: the engine gross behind a manual override, so ↺ can restore it). Applied via MCP as prod migration 'payments_computed_gross_php'. Backfill from the legacy note hit 3 of 1111 rows — the only overridden rows in prod; two recovered a genuinely different computed gross (36290 vs stored 31290, 35954.86 vs 38300), the third had already been collapsed to its override (25000) by the second-save bug. The other 287 non-null notes are 'Historical import (Hubstaff daily report)' and are NOT overrides.",
    "00000000000035": "2026-07-28 — narrowed 33 to app-locked periods. ⚠️ WRONG AND HARMFUL; SUPERSEDED BY 36 the same day. Two faults: (a) it was written on a wrong reading of pay_date, treating an early paid_at as evidence the SCHEDULE was period_end; (b) its predicate selected rows whose pay_date ALREADY equalled the arrears date and set them to period_end — i.e. it clobbered 33 correctly-dated prod periods, taking prod from 16 wrong rows to 49. It was recorded here as a 'no-op' at the time; that was a misreading of the verification count. 36 repairs it. Applied via MCP as prod migration 'pay_date_arrears_app_locked_only'.",
    "00000000000036": "2026-07-28 — pay_date is the SCHEDULED deadline, not a record of when money moved: payroll is paid at the latest on that date and in practice almost always earlier (manual payment), so an early paid_at says nothing about the schedule. Re-applies 33's arrears rule to every regular period, repairing 35's damage. 49 prod rows changed (16 originally wrong + 33 clobbered by 35), all `paid`, all pay_date = period_end, 2024-02-01 → 2026-04-01. Verified after: 0 regular periods with pay_date on or before period_end, the 1 off_cycle batch untouched, the open period keeps 2026-08-15. Applied via MCP as prod migration 'pay_date_schedule_all_periods'.",
    "00000000000037": "2026-08-01 — worker_companies_ended_requires_ended_on CHECK (#79). Backfill touched 5 links, all 'ended' with NULL ended_on, all on the employer company (Aaron Anderson E.H.S. LLC): Charminne Apura 2025-12-31, Kristine Anna Mei Malao 2025-03-06, Jecelyn Ferrer 2024-07-12, Danielle Joy Mistica 2024-07-11, Caren Divinagracia 2024-07-05. All five had started_on NULL, so every value came from branch (1) — their own last logged time entry, a fact, never a guess. Verified after: 0 'ended' links still unbounded, constraint present. Applied via MCP as prod migration 'worker_companies_ended_requires_ended_on'.",
    "00000000000038": "2026-08-01 — time_entries_no_time_after_last_day() + BEFORE INSERT/UPDATE triggers (#86). Applied AFTER the guarded hubstaff-sync went live (see 37/38 sequencing note in docs/DEPLOY.md), so the nightly bulk upsert drops post-last-day rows before the trigger can abort the whole statement. Pre-flight: 0 existing time_entries sat past their link's ended_on (checked after 37's backfill), so nothing pre-existing is now un-updatable. Verified live with a rolled-back probe insert against the most recently ended link (Angelika Faith Javier Alo, ended_on 2026-06-28): 2026-06-29 refused with the human-readable message + hint, 2026-06-28 itself accepted — the <= lastDay boundary the app-side guard uses. Applied via MCP as prod migration 'time_entries_no_time_after_last_day'.",
    "00000000000039": "2026-08-01 — portal-access-sunset cron (21:30 UTC) + workers_reactivation_restores_portal_login() trigger (#85, #90 B). ⚠️ The `app_base_url` row did NOT exist in prod at all — migration 0016 sits under baselineThrough but its cron/seed half was never applied here (prod's doc-expiry + hiring-review digests are the LEGACY edge-function crons, jobids 1-2, not this repo's routes). Left unset, `NULL || '/api/cron/portal-sunset'` is NULL and net.http_post would have errored nightly, so it was INSERTed as 'https://3a.abbilabs.com' (bare origin per docs/DEPLOY.md) before 39 was applied. Verified after: the job resolves to https://3a.abbilabs.com/api/cron/portal-sunset, is active, and the restore trigger is present. ✅ LIVE end-to-end since PR #96 merged (26145ee) the same day — /api/cron/portal-sunset 404'd until that deploy shipped the route. Proven by running the job's own command from inside Postgres (net.http_post, so the real request shape, secret never leaving the DB): HTTP 200 {\"ok\":true,\"checked\":2,\"revoked\":0} — which also proves app_secrets.cron_secret matches Vercel's CRON_SECRET, since a mismatch 401s. 2 departed contractors checked, 0 revoked: both still have pay outstanding, so the fail-safe held. contractor_logins unchanged at 10 active / 0 revoked. Applied via MCP as prod migration 'portal_access_sunset_cron_and_restore'.",
    "00000000000040": "2026-08-27 — workers.health_allowance_date (editable annual HA pay date; NULL = hire anniversary, month/day only, eligibility stays hire-keyed). Additive + nullable, applied before the code deploy. Verified after: column present, date, nullable. Applied via MCP as prod migration 'workers_health_allowance_date'.",
    "00000000000041": "2026-08-27 — pay_periods_off_cycle_open_uniq: the non-unique off-cycle open-batch index replaced by a UNIQUE partial index on (company_id) where kind='off_cycle' and state='open' (#131 — migration 24's one-open-batch invariant becomes a constraint; the app catches 23505 and adopts the winner). Pre-flight: ZERO employers with >1 open off-cycle batch, applied clean. Verified after: pay_periods_off_cycle_open_uniq present with indisunique=true, old pay_periods_off_cycle_open_idx gone. Applied via MCP as prod migration 'off_cycle_open_batch_unique'.",
    "00000000000042": "LOCAL-ONLY BY DESIGN — never apply to prod. Amends the payments_period_open_enforce trigger (migration 18), which is deliberately ABSENT from shared prod (the legacy apps still write payments there; the 2026-06-22 attempt broke them, see audit/CUTOVER-PLAN-2026-06-24.md §2.2). Verified 2026-08-28 via MCP: prod has only trg_payments_lock_enforce (BEFORE UPDATE), so outside-payment INSERTs need no prod-side change. Recorded here so the drift gate stops flagging it.",
    "00000000000043": "2026-08-29 — wise-reconcile cron (every 6h) → app route /api/cron/wise-reconcile (owner decision: Wise reconcile app-owned; supersedes migration 0016's never-applied edge-fn job, whose 'cron_reconcile' action the deployed v10 fn doesn't even accept — its cron actions are poll/match). Applied via MCP (cron.schedule only; no schema change). Verified post-deploy: the job's exact net.http_post returned HTTP 200 {ok:true,checked:0}. NOTE: file was briefly numbered 00000000000040, colliding with workers_health_allowance_date; renamed to 43 in PR #150."
  }
}
